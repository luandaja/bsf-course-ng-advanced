import { async, TestBed } from '@angular/core/testing';
import { Ui2Module } from './ui2.module';

describe('Ui2Module', () => {
  beforeEach(async(() => {
    TestBed.configureTestingModule({
      imports: [Ui2Module]
    }).compileComponents();
  }));

  it('should create', () => {
    expect(Ui2Module).toBeDefined();
  });
});
